"use client";

import CouncilTaxBillsBoard from "../../Shared/CouncilTaxBillsBoard";

// The board itself lives in Shared/CouncilTaxBillsBoard so add / edit / delete
// behave the same in every staff portal.
export default function AdminCouncilTaxBills() {
  return <CouncilTaxBillsBoard />;
}

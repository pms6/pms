"use client";

import MaintenanceBooklet from "../../Shared/MaintenanceBooklet";

// Same board as the admin portal — see Shared/MaintenanceBooklet.
export default function ManagerMaintenance() {
  return <MaintenanceBooklet subtitle="Repair issues, status and the solution taken" />;
}
